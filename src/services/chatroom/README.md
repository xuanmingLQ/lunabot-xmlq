# Chatroom Client for LunaBot

### Usage

1. Install dependencies (Python 3.8+ required):

```bash
pip install -r requirements.txt
```

2. Configure the service by editing the `config.yaml`.

3. Update the configuration of LunaBot by modifying `config/chatroom.yaml`.

4. Run the client:

```bash
python client.py
```

